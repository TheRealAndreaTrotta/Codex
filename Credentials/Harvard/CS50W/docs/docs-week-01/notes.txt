per clonare il path nella quale voglio pubblicare online la mia repo
git clone url...


git add file.exemple cosa fa?

per commentare un commit:
    git commit -m "message"

senza un utlimo passaggio i change avvengono unicamente in local
devo utilizzare il comando "git push".
In questo modo tutto ciò che ho fatto in precedenza su local verrà 
preso in considerazione e messo online

quindi, un corretto push online sarebbe:

    git commit -m "message"
    git push


se ho modificato più cose nel mio progetto e volessi vedere quali file sono stati 
modificati, mi basterebbe utilizzare:

    git status

per sapere le modifiche apportate rispetto all'ultimo git push

se volessi fare un commit di un singolo commit, allora uso il classico 
    git commit -m "etc etc"
ma se volessi fare un unico commit con tutti i file modificati allora uso:
    gt commit -am "message"


se volessi aggiornare la mia versione in locale con quella presente sul browser uso:
git pull

ma avrò dei conflitti come:
    hint: You have divergent branches and need to specify how to reconcile them.
hint: You can do so by running one of the following commands sometime before
hint: your next pull:
hint:
hint:   git config pull.rebase false  # merge
hint:   git config pull.rebase true   # rebase
hint:   git config pull.ff only       # fast-forward only
hint:
hint: You can replace "git config" with "git config --global" to set a default
hint: preference for all repositories. You can also pass --rebase, --no-rebase,
hint: or --ff-only on the command line to override the configured default per
hint: invocation.

spiegare le differenze tra rebase e merge

a cosa servono
git reset --hard
git reset --hard origina/master